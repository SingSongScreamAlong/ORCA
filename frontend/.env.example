# ORCA frontend configuration (template). Copy to .env.local.

# Base URL of the ORCA backend API. Used by both server and client code, so it must
# be reachable from the browser.
NEXT_PUBLIC_API_BASE=http://localhost:8000/api/v1
