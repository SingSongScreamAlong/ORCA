"""Pydantic schemas — the API contract.

Request models are suffixed ``Create``; response models carry the full record. These
mirror ``ontology/schema`` and are the read/write shapes the API exposes.
"""
