"""Hunting Grounds — the future collection engine (interfaces only).

This package defines *interfaces* for collection. No collection logic is implemented.
A collector, when built, is an ordinary producer of observations and evidence through
the same audited, reviewed path an analyst uses — it has no privileged route that
bypasses review or audit.

See ``app/collection/interfaces.py`` and ``docs/roadmap.md`` (Phase 5).
"""
